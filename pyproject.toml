[project]
name = "dataops"
version = "0.1.0"
description = "DataTalksClub DataOps — content validation and planning docs tooling."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
